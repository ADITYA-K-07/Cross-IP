import { ComingSoonPage } from "@/components/ip/ComingSoonPage";

export default function FtoPage() {
  return (
    <ComingSoonPage
      title="Freedom to Operate"
      description="Check whether a product may conflict with active patents before launch."
      plan="Startup"
    />
  );
}
